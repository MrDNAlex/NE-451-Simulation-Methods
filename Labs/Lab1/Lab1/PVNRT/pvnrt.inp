# Define Variables
variable temp equal 300

# Define the Units
units lj
atom_style atomic

# Create a Simulation Box



lattice fcc 0.8442
region box block 0 10 0 10 0 10
create_box 1 box
create_atoms 1 box

# Assign the Group to Argon and Assign it's mass
group Ar type 1
mass 1 39.948

# Define the Potential of the Simulation
pair_style lj/cut 2.5
pair_coeff 1 1 1.0 1.0

# Set the initial velocity
velocity all create ${temp} 54343

# Define the Thermodynamics Output
thermo 100
thermo_style custom step temp press vol etotal

# Compute the RDF
compute RDF all rdf 100

# Run the Simulation
fix integrate1 all nvt temp ${temp} ${temp} 10.0
timestep 0.005
run 10000

unfix integrate1

# Output the RDF Data to the Dump Folder
fix integrate2 all nvt temp ${temp} ${temp} 10.0
fix 1 all ave/time 100 10 1000 c_RDF[*] file RDF.data mode vector

run 50000

unfix integrate2